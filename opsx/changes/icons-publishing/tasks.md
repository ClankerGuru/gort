# Icons Publishing Tasks

## Tasks

- [x] ✅ `ip-1` agent:github retries:2 — Update `:icons` module `build.gradle.kts` to publish as `zone.clanker:gort-icons` artifact. Change the publishing coordinates in the POM to use `artifactId = "gort-icons"`, `groupId = "zone.clanker"`. Remove the `signing` block that uses `useGpgCmd()` and replace with the same pattern as `:gort` module. Ensure the `maven-publish` and `signing` plugins are applied. File: `icons/build.gradle.kts`. Verify: `./gradlew :icons:tasks --group=publishing` shows publish tasks.

- [~] ⛔ `ip-2` depends:ip-1 agent:github retries:2 — Update `:gort` module to depend on published `zone.clanker:gort-icons` instead of `project(":icons")`. In `gort/build.gradle.kts`, change `api(project(":icons"))` to `api("zone.clanker:gort-icons:$version")` where version comes from the project version. Add `gort-icons` to the version catalog in `gradle/libs.versions.toml` as `gortIcons = { module = "zone.clanker:gort-icons", version = "0.1.0" }` and reference it as `api(libs.gortIcons)`. File: `gort/build.gradle.kts`, `gradle/libs.versions.toml`. Verify: `./gradlew :gort:dependencies` shows `zone.clanker:gort-icons`.

- [x] ✅ `ip-3` depends:ip-2 agent:github retries:2 — Update the Gort release workflow to publish both `:gort` and `:icons` modules. In `.github/workflows/release.yml`, change the publish step from `:gort:publishAllPublicationsToMavenCentralRepository` to publish both modules: first `:icons:publishAllPublicationsToMavenCentralRepository` then `:gort:publishAllPublicationsToMavenCentralRepository` (icons must publish first since gort depends on it). File: `.github/workflows/release.yml`. Verify: workflow YAML is valid.

- [x] ✅ `fs-1` agent:github retries:2 — Fix the signing configuration in `icons/build.gradle.kts` to match exactly the pattern used in `gort/build.gradle.kts`. Currently `:icons` uses `useInMemoryPgpKeys(signingKey, signingPassword)` with `SIGNING_KEY`/`SIGNING_PASSWORD` env vars. It MUST use `useGpgCmd()` with `GPG_PASSPHRASE` env var instead — exactly like `:gort`. Read `gort/build.gradle.kts` signing block as reference: `signing { val gpgPassphrase = providers.environmentVariable("GPG_PASSPHRASE").orNull; if (gpgPassphrase != null) { useGpgCmd() }; sign(publishing.publications) }` and `tasks.withType<Sign>().configureEach { onlyIf { providers.environmentVariable("GPG_PASSPHRASE").isPresent } }`. Copy that exact pattern into `icons/build.gradle.kts`, replacing the current signing block. File: `icons/build.gradle.kts`. Verify: `grep -A10 'signing {' icons/build.gradle.kts` shows `useGpgCmd()`.

- [/] 🔄 `og-1` agent:github retries:2 — Add OS detection to `icons/build.gradle.kts` so native Apple targets (iOS and macOS) are only registered when building on macOS. Linux and Windows machines should skip those targets entirely. Use `org.gradle.internal.os.OperatingSystem.current().isMacOsX` to check. Wrap the iOS `listOf(iosArm64(), iosSimulatorArm64())` block AND the macOS `listOf(macosArm64(), macosX64())` block inside `if (OperatingSystem.current().isMacOsX) { ... }`. Add the import `import org.gradle.internal.os.OperatingSystem` at the top. The JVM, Android, WASM, and JS targets must remain unconditional — they work on all platforms. File: `icons/build.gradle.kts`. Verify: on this Linux machine, `./gradlew :icons:assemble --no-daemon` succeeds without trying to compile native targets.
  > **Attempt 1** (2026-03-21 02:58): 
[TIMEOUT after 300s]
