---
name: "OPSX: co-1"
description: "Add Navigation 3 dependency to catalog module and replace enum-based navigation with sealed class routes and NavDisplay (catalog-overhaul)"
category: Task
tags: [task, catalog-overhaul]
---

🔄 **co-1**: Add Navigation 3 dependency to catalog module and replace enum-based navigation with sealed class routes and NavDisplay

**Proposal:** catalog-overhaul

## Context

Read these files before starting:
- `opsx/changes/catalog-overhaul/proposal.md` — what & why
- `opsx/changes/catalog-overhaul/design.md` — how
- `opsx/changes/catalog-overhaul/tasks.md` — all tasks & progress

## Implementation

1. Read the context files above
2. Implement this task: **Add Navigation 3 dependency to catalog module and replace enum-based navigation with sealed class routes and NavDisplay**
3. When complete, mark done: `./gradlew opsx-co-1 --set=done`

## ⚠️ Reconciliation Warning

This task references symbols not found in the codebase:
- **`Navigation`** — not found. Did you mean: `g`, `NavigationScreen`, `a`?
- **`NavDisplay`** — not found. Did you mean: `s`, `l`, `a`?

Review and update this task if the referenced code has changed.

